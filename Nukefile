;; Nukefile for Nu demo application

;; source files
(set @nu_files 	  (filelist "^nu/.*nu$"))

;; application description
(set @application 	       	   "WordsApp")
(set @application_identifier   "com.ey_office.wordsapp")

;; tasks
(application-tasks)
(task "default" => "application")
